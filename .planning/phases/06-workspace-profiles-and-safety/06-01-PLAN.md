---
phase: 06-workspace-profiles-and-safety
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [src/types.ts, src/settings.ts, src/workspaceCatalog.ts, src/repositoryIdentity.ts, test/workspaceCatalog.test.ts, test/settings.test.ts, test/repositoryIdentity.test.ts]
autonomous: true
requirements: [PROF-01, PROF-07, PROF-08, PROF-09, PROF-10]
user_setup: []
must_haves:
  truths:
    - Workspace profiles have stable IDs, names, Host/WSL paths, lifecycle state, enabled, trusted, and preferred attributes.
    - Large profile sets are searchable and cursor-paginated without duplicates or unstable ordering.
    - Preferred affects ordering, while trusted independently controls safety decisions.
    - Soft delete, restore, locate, rename, and permanent delete preserve deterministic lifecycle rules.
    - Repository identity checks do not require Git and fail closed on mismatch.
  artifacts: [src/workspaceCatalog.ts, src/repositoryIdentity.ts, test/workspaceCatalog.test.ts]
  key_links:
    - settings normalization persists the catalog model without creating a second store.
    - tools consume catalog operations rather than mutating arrays directly.
---

<objective>
Build the durable workspace catalog, lifecycle, search/pagination, trust/preference, and repository identity foundation.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-workspace-profiles-and-safety/CONTEXT.md
@.planning/phases/06-workspace-profiles-and-safety/06-RESEARCH.md
@src/types.ts
@src/settings.ts
@src/workspaceSelection.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend the durable workspace profile and catalog model</name>
  <files>src/types.ts, src/settings.ts, src/workspaceCatalog.ts, test/workspaceCatalog.test.ts, test/settings.test.ts</files>
  <read_first>src/types.ts, src/settings.ts, src/workspaceSelection.ts, test/settings.test.ts</read_first>
  <behavior>
    - Legacy profiles normalize with safe defaults for enabled, trusted, preferred, lifecycle, and timestamps.
    - Search is case-insensitive across name and environment-specific paths.
    - Cursor pagination is stable, preferred profiles sort first, and pages contain no duplicates.
    - Add, rename, locate, soft delete, restore, and permanent delete preserve stable profile IDs.
    - Trusted and preferred can be changed independently.
  </behavior>
  <action>Extend `WorkspaceProfile` and settings normalization, then implement a pure `WorkspaceCatalog` service with deterministic sorting, opaque cursor pagination, search, add/update/lifecycle methods, and workspace-enabled global state. Keep all mutations immutable and persistence-neutral so `settings.ts` remains the single durable store.</action>
  <acceptance_criteria>
    - A 500-profile fixture can be searched and paged deterministically with no duplicate IDs.
    - Soft-deleted profiles are excluded by default and recoverable through restore.
    - Permanent deletion rejects the active profile unless explicitly cleared by the caller.
    - Trusted/preferred/enabled flags round-trip through settings normalization.
    - `npm test -- --test-name-pattern="workspace catalog|workspace pagination|trusted workspace|settings"` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="workspace catalog|workspace pagination|trusted workspace|settings"</automated></verify>
  <done>The project has one backward-compatible, scalable workspace catalog model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add advisory repository identity validation</name>
  <files>src/repositoryIdentity.ts, src/types.ts, test/repositoryIdentity.test.ts</files>
  <read_first>src/workspace.ts, src/workspaceSelection.ts, src/preflight.ts, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Identity can be derived without requiring Git.
    - Matching identity succeeds; absent identity remains advisory; mismatched identity fails closed.
    - Host and WSL identity facts are supplied by the selected backend rather than Host-only filesystem assumptions.
  </behavior>
  <action>Create a backend-neutral repository identity contract based on stable advisory facts such as normalized root marker, optional configured metadata, and caller-supplied backend facts. Keep derivation pure. Add compare/validate helpers that return structured match, unknown, or mismatch outcomes without shelling out to Git.</action>
  <acceptance_criteria>
    - Tests cover match, unknown, and mismatch outcomes.
    - No implementation path invokes Git.
    - Identity mismatch can be consumed by Phase 5 preflight/recovery contracts.
    - `npm test -- --test-name-pattern="repository identity|workspace catalog" && npm run build` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="repository identity|workspace catalog" && npm run build</automated></verify>
  <done>Workspace profiles can be revalidated without adding a Git dependency.</done>
</task>
</tasks>

<verification>
- [ ] Catalog, settings, and identity tests pass.
- [ ] `npm run build` passes.
- [ ] `git diff --check` passes.
</verification>

## Artifacts this phase produces

- Extended durable `WorkspaceProfile`
- `src/workspaceCatalog.ts`
- `src/repositoryIdentity.ts`
- Catalog, pagination, lifecycle, trust/preference, and identity tests

<output>After completion, create `06-01-SUMMARY.md`.</output>
