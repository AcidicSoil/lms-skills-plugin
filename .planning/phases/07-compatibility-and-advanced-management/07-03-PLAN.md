---
phase: 07-compatibility-and-advanced-management
plan: 03
type: standard
wave: 3
depends_on: [07-01, 07-02]
files_modified: [src/types.ts, src/chatWorkspaceState.ts, src/sessionCapability.ts, src/toolsProvider.ts, src/pluginTypes.ts, test/chatWorkspaceState.test.ts, test/workspaceConfiguration.integration.test.ts, test/compatibility.test.ts]
autonomous: true
requirements: [COMP-05, COMP-06, TEST-02, TEST-07]
user_setup: []
must_haves:
  truths:
    - Per-chat environment and workspace selection persist through plugin-owned state independent of transcript/session APIs.
    - Provider recreation restores the correct chat selection without leaking another chat's state.
    - Resume is unavailable by default and appears only when an explicit stable host capability is supplied.
    - No transcript text or duplicated conversation data is stored as a restoration workaround.
    - Full migration, malformed-data, downgrade, and non-Windows startup coverage passes.
  artifacts: [src/chatWorkspaceState.ts, src/sessionCapability.ts, test/chatWorkspaceState.test.ts]
  key_links:
    - toolsProvider resolves chat selection through chatWorkspaceState before workspace/backend construction.
    - resume tools delegate only to sessionCapability and remain absent/unsupported by default.
---

<objective>
Persist and restore per-chat workspace selection independently of host sessions, capability-gate resume behavior, and complete milestone verification.
</objective>

<context>
@.planning/phases/07-compatibility-and-advanced-management/07-01-SUMMARY.md
@.planning/phases/07-compatibility-and-advanced-management/07-02-SUMMARY.md
@.planning/phases/07-compatibility-and-advanced-management/07-RESEARCH.md
@src/toolsProvider.ts
@src/settings.ts
@src/pluginTypes.ts
@test/workspaceConfiguration.integration.test.ts
@test/compatibility.test.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add plugin-owned per-chat workspace selection restoration</name>
  <files>src/types.ts, src/chatWorkspaceState.ts, src/settings.ts, src/toolsProvider.ts, test/chatWorkspaceState.test.ts, test/workspaceConfiguration.integration.test.ts</files>
  <read_first>src/toolsProvider.ts, src/settings.ts, src/types.ts, test/workspaceConfiguration.integration.test.ts</read_first>
  <behavior>
    - A stable caller-supplied chat ID maps to environment and active workspace profile selection.
    - Two chats can retain different selections without leakage.
    - Provider recreation restores each chat's last valid selection.
    - Missing, deleted, disabled, or invalid restored profiles produce safe configuration-required state rather than fallback.
  </behavior>
  <action>Create a plugin-owned `ChatWorkspaceStateStore` persisted through settings or a dedicated plugin data file, keyed only by stable chat ID and containing environment/profile IDs plus timestamps. Integrate it into provider initialization and successful switching. Do not reference transcript messages or host session objects. Validate restored profiles before activation and preserve safe rollback behavior.</action>
  <acceptance_criteria>
    - Integration tests recreate providers for two chats and restore distinct selections.
    - Invalid restored selections fail closed without home/cwd fallback.
    - No persisted type contains transcript/message content.
    - `npm test -- --test-name-pattern="chat workspace restoration|selection persistence|workspace switch"` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="chat workspace restoration|selection persistence|workspace switch"</automated></verify>
  <done>Workspace persistence is chat-aware and independent of host session APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit session-resume capability gating</name>
  <files>src/sessionCapability.ts, src/pluginTypes.ts, src/toolsProvider.ts, test/chatWorkspaceState.test.ts, test/compatibility.test.ts</files>
  <read_first>src/pluginTypes.ts, src/toolsProvider.ts, node_modules/@lmstudio/sdk/dist/index.d.ts, .planning/phases/07-compatibility-and-advanced-management/07-RESEARCH.md</read_first>
  <behavior>
    - Default capability reports resume unsupported and performs no session action.
    - A stable injected host adapter can expose resume availability and execute resume by opaque session reference.
    - Transcript duplication, message copying, and fabricated session reconstruction are absent.
  </behavior>
  <action>Create a `SessionCapability` discriminated adapter with unsupported and supported implementations. Expose status and resume operations only through this adapter; default to unsupported for the installed SDK. Use opaque stable references only and reject any payload containing transcript/message content. Keep workspace persistence functional regardless of resume capability.</action>
  <acceptance_criteria>
    - Default tests assert resume unavailable and no host calls.
    - Injected supported adapter tests assert capability and opaque-reference delegation.
    - Source/tests contain no transcript-copy fallback implementation.
    - `npm test -- --test-name-pattern="session capability|resume unavailable|transcript|compatibility" && npm run build` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="session capability|resume unavailable|transcript|compatibility" && npm run build</automated></verify>
  <done>Resume behavior is honest, capability-gated, and decoupled from workspace persistence.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 7 and milestone release verification</name>
  <files>test/compatibility.test.ts, test/workspaceConfiguration.integration.test.ts, test/settingsMigration.test.ts</files>
  <read_first>package.json, scripts/verify-release.mjs, .planning/phases/07-compatibility-and-advanced-management/07-VALIDATION.md</read_first>
  <action>Close any missing validation-matrix assertions and run the full test, build, release, and diff gates. Confirm all intentional public tool additions are represented in compatibility coverage and all Phase 7 requirement IDs have behavioral evidence.</action>
  <acceptance_criteria>
    - All eight Phase 7 requirements have behavioral test evidence.
    - `npm test`, `npm run build`, `npm run verify:release`, and `git diff --check` all exit 0.
  </acceptance_criteria>
  <verify><automated>npm test && npm run build && npm run verify:release && git diff --check</automated></verify>
  <done>Phase 7 and milestone v1.1 are release-ready with no unresolved compatibility gaps.</done>
</task>
</tasks>

<verification>
- [ ] Chat restoration and session-capability tests pass.
- [ ] Full suite, build, release verification, and diff checks pass.
- [ ] No unresolved plan-checker findings remain.
</verification>

## Artifacts this phase produces

- `src/chatWorkspaceState.ts`
- `src/sessionCapability.ts`
- Per-chat restoration integration tests
- Capability-gated resume tests
- Final compatibility and migration verification

<output>After completion, create `07-03-SUMMARY.md`.</output>
