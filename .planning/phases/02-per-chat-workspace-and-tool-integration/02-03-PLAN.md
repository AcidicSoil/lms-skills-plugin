---
phase: 02-per-chat-workspace-and-tool-integration
plan: 03
type: tdd
wave: 3
depends_on: ["02-01", "02-02"]
files_modified: [src/toolsProvider.ts, test/toolsProvider.integration.test.ts]
autonomous: true
requirements: [TOOL-02, TOOL-03, TEST-02]
user_setup: []
must_haves:
  truths:
    - Project file tools and run_command use the same resolved workspace root.
    - Skill tools never route through project workspace policy.
    - Users can inspect active workspace metadata.
    - Host and testable WSL end-to-end flows pass.
  artifacts:
    - src/toolsProvider.ts
    - test/toolsProvider.integration.test.ts
  key_links:
    - toolsProvider resolves one WorkspaceContext from controller/settings and passes it to workspaceFs and executor.
    - Skill tools retain scanner/configured-root code paths.
---

<objective>
Migrate project-scoped tools to the shared workspace context and prove end-to-end Host/WSL integration without changing skill-root behavior.

Purpose: Deliver the approved user story as an observable vertical slice.
Output: Workspace-aware tools, inspection output, integration tests, and synchronized build.
</objective>

<execution_context>
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/workflows/execute-plan.md
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-per-chat-workspace-and-tool-integration/02-01-SUMMARY.md
@.planning/phases/02-per-chat-workspace-and-tool-integration/02-02-SUMMARY.md
@src/toolsProvider.ts
@src/workspace.ts
@src/workspaceFs.ts
@src/scanner.ts
@src/executor.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Resolve one workspace context per tools provider and expose inspection</name>
  <files>src/toolsProvider.ts, test/toolsProvider.integration.test.ts</files>
  <read_first>src/toolsProvider.ts, src/workspace.ts, src/settings.ts, src/pluginTypes.ts</read_first>
  <action>Resolve effective config and workspace context from the provider controller through a shared lazy/idempotent helper. Update `get_current_directory` to return workspace ID, provider identity, environment, distribution, and native root. Ensure resolution errors are returned consistently by project-scoped tools.</action>
  <verify>npm test -- --test-name-pattern="workspace inspection"</verify>
  <acceptance_criteria>
    - Inspection reports the exact context used by operations.
    - Repeated tool calls for one controller reuse the same deterministic context.
    - No per-call tool ID influences workspace selection.
  </acceptance_criteria>
  <done>Workspace context is visible and shared.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate project-scoped file tools and run_command</name>
  <files>src/toolsProvider.ts, test/toolsProvider.integration.test.ts</files>
  <read_first>src/toolsProvider.ts, src/workspaceFs.ts, src/executor.ts</read_first>
  <action>Route read/write/patch/append/create/list/delete/move/rename through `workspaceFs`. Update schemas/descriptions so relative paths are workspace-relative and contained absolute paths remain valid. Force `run_command` cwd to the same workspace root by default; if a caller supplies cwd, resolve and contain it through the workspace service before execution. Preserve model-facing result shapes where practical and add workspace metadata where useful.</action>
  <verify>npm test -- --test-name-pattern="project tools workspace"</verify>
  <acceptance_criteria>
    - File operations and commands observe the same created files/root.
    - No project-scoped tool directly calls arbitrary `path.resolve` plus Node mutation outside workspaceFs.
    - Command cwd cannot escape the workspace.
  </acceptance_criteria>
  <done>TOOL-02 is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Prove skill-boundary separation and Host/WSL integration</name>
  <files>src/toolsProvider.ts, test/toolsProvider.integration.test.ts</files>
  <read_first>src/toolsProvider.ts, src/scanner.ts, test/toolsProvider.integration.test.ts</read_first>
  <action>Add integration tests for a Host create/write/read/patch/list/run/move/delete flow and a WSL flow using injected workspace/execution backends. Spy on workspace resolution to prove `list_skills`, `read_skill_file`, and `list_skill_files` retain configured skill-root behavior. Run full build/tests and inspect diffs for accidental skill-tool changes.</action>
  <verify>npm test && npm run build && git diff --check</verify>
  <acceptance_criteria>
    - Host and WSL integration paths pass.
    - Skill tools do not invoke workspaceFs or rewrite skill roots.
    - All existing Phase 1 tests remain green.
    - Generated output is rebuilt locally according to repository policy.
  </acceptance_criteria>
  <done>TOOL-03 and TEST-02 are verified.</done>
</task>
</tasks>

<verification>
- [ ] npm test
- [ ] npm run build
- [ ] git diff --check
- [ ] Skill-library operations remain outside workspace routing.
</verification>

<success_criteria>
- Approved MVP user story works through one deterministic Host/WSL workspace.
- Every project tool shares the same root.
- Skill boundaries are unchanged.
- Integration tests prove both environment paths.
</success_criteria>

<output>
After completion, create `.planning/phases/02-per-chat-workspace-and-tool-integration/02-03-SUMMARY.md`
</output>
