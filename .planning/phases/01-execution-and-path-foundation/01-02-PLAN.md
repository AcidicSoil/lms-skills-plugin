---
phase: 01-execution-and-path-foundation
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified: [src/pathPolicy.ts, test/pathPolicy.test.ts]
autonomous: true
requirements: [PATH-01, PATH-02, PATH-03, PATH-04]
user_setup: []
must_haves:
  truths:
    - Paths are classified before use and wrong-environment absolute paths are rejected.
    - Relative paths resolve only against an explicit root.
    - Canonical containment rejects traversal and symlink/junction escape.
  artifacts:
    - src/pathPolicy.ts
    - test/pathPolicy.test.ts
  key_links:
    - Path policy exposes pure classification plus injectable canonicalization.
---

<objective>
Build the environment-aware path and containment primitives required by Host and WSL execution.

Purpose: Remove guessed translation and string-prefix boundary checks from future workspace routing.
Output: Pure path policy module and adversarial automated tests.
</objective>

<execution_context>
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/workflows/execute-plan.md
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-execution-and-path-foundation/01-CONTEXT.md
@.planning/phases/01-execution-and-path-foundation/01-RESEARCH.md
@src/scanner.ts
@src/executor.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Test-drive explicit path classification and root resolution</name>
  <files>src/pathPolicy.ts, test/pathPolicy.test.ts</files>
  <read_first>src/scanner.ts, src/executor.ts, .planning/phases/01-execution-and-path-foundation/01-CONTEXT.md</read_first>
  <action>Create typed classification for relative, Windows-drive absolute, WSL UNC absolute, Linux absolute, and invalid paths. Implement environment validation that rejects wrong-environment absolute paths; do not silently convert Windows paths to `/mnt/*`. Resolve relative paths only when an explicit native root is supplied. Cover spaces, Unicode, drive-letter case, mixed/trailing separators, `.` and `..`.</action>
  <verify>npm test -- --test-name-pattern="path"</verify>
  <acceptance_criteria>
    - src/pathPolicy.ts exports explicit path-kind and validation result types.
    - Linux paths are rejected for Host-Windows use and Windows paths are rejected for WSL use unless an explicit supported mapping API is called.
    - Missing roots never resolve to the user's home directory.
  </acceptance_criteria>
  <done>PATH-01 through PATH-03 have deterministic tests.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce canonical containment</name>
  <files>src/pathPolicy.ts, test/pathPolicy.test.ts</files>
  <read_first>src/pathPolicy.ts, src/scanner.ts</read_first>
  <action>Add a containment function that compares canonical root and target with separator boundaries and platform-aware case semantics. Make canonicalization injectable so tests simulate realpath, symlinks, and Windows junctions. Equality with the root is allowed; sibling-prefix paths and targets escaping through canonical links are rejected.</action>
  <verify>npm test -- --test-name-pattern="containment" && npm run build</verify>
  <acceptance_criteria>
    - Tests reject `root-evil`, `../`, and canonical symlink/junction escape cases.
    - Tests allow the root itself and true descendants.
    - No containment decision relies on a bare `startsWith(root)` check.
  </acceptance_criteria>
  <done>PATH-04 is covered by adversarial tests.</done>
</task>
</tasks>

<verification>
- [ ] npm test
- [ ] npm run build
- [ ] Path tests pass on non-Windows CI without accessing WSL.
</verification>

<success_criteria>
- All path requirements are covered.
- Wrong-environment paths fail closed.
- Canonical containment handles traversal and link escapes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-execution-and-path-foundation/01-02-SUMMARY.md`
</output>
