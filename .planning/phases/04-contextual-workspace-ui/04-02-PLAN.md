---
phase: 04-contextual-workspace-ui
plan: 02
type: standard
wave: 2
depends_on: [04-01]
files_modified: [src/config.ts, src/settings.ts, src/wslCapability.ts, src/toolsProvider.ts, test/settings.test.ts, test/wslCapability.test.ts, test/toolsProvider.integration.test.ts]
autonomous: true
requirements: [UI-04, UI-05, UI-06, UI-07]
user_setup: []
must_haves:
  truths:
    - Host mode exposes only Host workspace controls and WSL mode exposes only WSL workspace/distribution controls in the contextual tool surface.
    - Empty WSL distribution means use the system default distribution.
    - A non-empty distribution override is validated before use.
    - WSL capability states remain actionable and refreshable.
  artifacts:
    - src/config.ts
    - src/wslCapability.ts
    - test/wslCapability.test.ts
  key_links:
    - Static config descriptions match the runtime settings semantics.
    - WSL override validation reuses the existing capability detector.
---

<objective>
Implement an environment-aware workspace configuration surface using dynamic tools, while keeping static LM Studio schematics limited to bootstrap defaults.

Purpose: Satisfy UI-04 without relying on unsupported conditional config fields, and make default-distribution and override behavior explicit and validated.
Output: Minimal static config, Host-only and WSL-only contextual tools, capability contracts, and regression tests.
</objective>

<execution_context>
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/workflows/execute-plan.md
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-contextual-workspace-ui/CONTEXT.md
@.planning/phases/04-contextual-workspace-ui/04-RESEARCH.md
@.planning/phases/04-contextual-workspace-ui/04-01-SUMMARY.md
@src/config.ts
@src/settings.ts
@src/wslCapability.ts
@test/settings.test.ts
@test/wslCapability.test.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build environment-specific workspace configuration tools</name>
  <files>src/config.ts, src/toolsProvider.ts, test/toolsProvider.integration.test.ts</files>
  <read_first>src/config.ts, src/toolsProvider.ts, src/settings.ts, src/workspaceSelection.ts, test/toolsProvider.integration.test.ts, node_modules/@lmstudio/sdk/dist/index.d.ts</read_first>
  <behavior>
    - Host mode registers a Host workspace configuration tool and does not register WSL-only controls.
    - WSL mode registers a WSL workspace configuration tool and does not register Host-only shell/path controls.
    - The environment switch and stable bootstrap defaults remain available through static per-chat config.
    - Configuration mutations return the updated active selection and status contract.
  </behavior>
  <action>Reduce static config schematics to the stable bootstrap fields that LM Studio can represent honestly. In `toolsProvider`, construct the returned tool list from the current per-chat execution environment: expose a typed Host workspace configuration tool in Host mode and a typed WSL workspace/distribution configuration tool in WSL mode. Omit irrelevant tools entirely. Reuse settings normalization and the workspace-selection model; do not create parallel persistence. Add integration tests proving the tool surface changes with the selected environment.</action>
  <acceptance_criteria>
    - Host mode tool registration includes the Host workspace configuration tool and excludes the WSL workspace/distribution configuration tool.
    - WSL mode tool registration includes the WSL workspace/distribution configuration tool and excludes Host-only workspace/shell controls.
    - `src/config.ts` retains only stable bootstrap fields and does not claim conditional visibility support.
    - Configuration tools persist through the existing settings path and return the updated workspace-status contract.
    - `npm test -- --test-name-pattern="environment-specific workspace configuration|tools provider" && npm run build` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="environment-specific workspace configuration|tools provider" && npm run build</automated></verify>
  <done>Irrelevant Host/WSL controls are absent from the active contextual configuration surface, satisfying UI-04 within supported SDK primitives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete WSL capability and override-state contracts</name>
  <files>src/wslCapability.ts, src/settings.ts, test/wslCapability.test.ts, test/settings.test.ts</files>
  <read_first>src/wslCapability.ts, src/settings.ts, test/wslCapability.test.ts, test/settings.test.ts</read_first>
  <behavior>
    - Empty override with a default distribution reports ready.
    - Empty override with no default reports `no-default-distribution`.
    - A missing named override reports `selected-distribution-unavailable`.
    - Missing executable, no distributions, and pending detection remain distinct states.
    - Refresh performs a new probe without changing persisted selection.
  </behavior>
  <action>Extend the existing WSL capability discriminated union and detector only where required to represent all UI-06 states. Add an explicit refreshable probe seam whose return value is transient and does not mutate settings. Validate non-empty overrides against installed distributions; preserve empty as system-default semantics. Return stable machine-readable codes plus actionable user messages.</action>
  <acceptance_criteria>
    - `src/wslCapability.ts` returns distinct codes for ready, executable missing, no distributions, no default distribution, selected distribution unavailable, and pending detection.
    - Empty override selects the detected default distribution; non-empty override must match an installed distribution.
    - Refresh returns a new probe result without writing settings.
    - `npm test -- --test-name-pattern="WSL capability|settings" && npm run build` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="WSL capability|settings" && npm run build</automated></verify>
  <done>Default and overridden WSL distribution behavior is fully represented, validated, and test-covered.</done>
</task>
</tasks>

<verification>
- [ ] npm test -- --test-name-pattern="WSL capability|settings"
- [ ] npm run build
- [ ] git diff --check
</verification>

<success_criteria>
- UI-04 through UI-07 are implemented without inventing unsupported SDK behavior.
- All required WSL capability states are machine-readable and actionable.
- Refresh does not implicitly alter user configuration.
</success_criteria>

## Artifacts this phase produces

- Environment-specific Host and WSL workspace configuration tools in `src/toolsProvider.ts`
- Updated bootstrap config schematics in `src/config.ts`
- Extended WSL capability-state union and refresh probe seam in `src/wslCapability.ts`
- Integration coverage for environment-specific tool registration

<output>
After completion, create `.planning/phases/04-contextual-workspace-ui/04-02-SUMMARY.md`
</output>
