{
  "name": "lms-plugin-skills",
  "version": "1.0.7",
  "description": "Claude-style skill system for LM Studio. Discovers skill directories, injects an available-skills list into every prompt, and provides tools to read skill files - mirroring exactly how Claude's own skill system works.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc && lms dev",
    "push": "lms push",
    "test": "node scripts/test.mjs",
    "verify:release": "node scripts/verify-release.mjs",
    "format": "prettier src --write --cache",
    "format:check": "prettier . --check --cache"
  },
  "author": "khtsly",
  "license": "Apache-2.0",
  "dependencies": {
    "@lmstudio/sdk": "^1.5.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^20.14.8",
    "prettier": "3.9.5",
    "typescript": "^5.4.5"
  }
}