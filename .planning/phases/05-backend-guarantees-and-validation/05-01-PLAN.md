---
phase: 05-backend-guarantees-and-validation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [src/executor.ts, src/preflight.ts, src/recoveryError.ts, src/types.ts, test/executor.test.ts, test/preflight.test.ts]
autonomous: true
requirements: [BACK-04, BACK-05, BACK-06, BACK-07, BACK-08, TEST-08]
user_setup: []
must_haves:
  truths:
    - Raw shell strings and structured program/argument execution are distinct typed contracts.
    - WSL raw commands always use the WSL Bash shell and ignore Host shell settings.
    - Arbitrary raw strings are never tokenized into argv.
    - Environment, distribution, workspace, and tool compatibility fail before invocation.
    - Preflight failures have stable machine-readable recovery codes and actions.
  artifacts: [src/preflight.ts, src/recoveryError.ts, test/preflight.test.ts]
  key_links:
    - tools consume preflight results before calling executor or filesystem operations.
    - executor tests assert exact spawn program and argv for Host and WSL modes.
---

<objective>
Formalize execution contracts and create the shared preflight/recovery-error foundation.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-backend-guarantees-and-validation/CONTEXT.md
@.planning/phases/05-backend-guarantees-and-validation/05-RESEARCH.md
@src/executor.ts
@src/workspaceSelection.ts
@src/wslCapability.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Lock raw and structured execution semantics</name>
  <files>src/executor.ts, src/types.ts, test/executor.test.ts</files>
  <read_first>src/executor.ts, test/executor.test.ts, src/pathEnvironment.ts</read_first>
  <behavior>
    - WSL raw execution spawns `wsl.exe` with `--exec /bin/bash -lc` and the original command as one argument.
    - WSL structured execution spawns `wsl.exe --exec program ...args` without Bash.
    - Host shell settings change Host raw execution only.
    - Embedded spaces, quotes, pipes, and redirects remain unchanged in the raw command argument.
  </behavior>
  <action>Add explicit raw-command and structured-program request types and refactor executor construction around them without changing supported public behavior. Expand exact-argv tests across Host cmd/PowerShell/POSIX and WSL default/named distributions. Do not add a tokenizer or split raw command strings.</action>
  <acceptance_criteria>
    - Exact spawn program/argv tests cover every behavior above.
    - No raw execution path calls a tokenization helper.
    - `npm test -- --test-name-pattern="execution contract|executor"` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="execution contract|executor"</automated></verify>
  <done>Execution semantics are explicit, stable, and regression-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add shared preflight and structured recovery errors</name>
  <files>src/preflight.ts, src/recoveryError.ts, src/types.ts, test/preflight.test.ts</files>
  <read_first>src/workspaceSelection.ts, src/wslCapability.ts, src/toolsProvider.ts, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Invalid workspace, unavailable environment, missing distribution, incompatible tool, denied approval, identity mismatch, and unresolved termination map to stable codes.
    - Failures include a human message and one or more recovery actions.
    - Valid contexts return a typed invocation-ready result.
  </behavior>
  <action>Create a discriminated `RecoveryError` contract and pure preflight validator. Model tool compatibility input now, while allowing approval/identity/termination facts to be supplied by later phases. Keep the validator side-effect free and ensure all failures are determined before invocation.</action>
  <acceptance_criteria>
    - Every BACK-08 category has a stable code and test.
    - The validator returns no executable context for any failure.
    - `npm test -- --test-name-pattern="preflight|structured recovery" && npm run build` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="preflight|structured recovery" && npm run build</automated></verify>
  <done>One reusable preflight/error contract is ready for all workspace-facing tools.</done>
</task>
</tasks>

<verification>
- [ ] Focused executor and preflight tests pass.
- [ ] `npm run build` passes.
- [ ] `git diff --check` passes.
</verification>

## Artifacts this phase produces

- Typed raw and structured execution requests
- `src/preflight.ts`
- `src/recoveryError.ts`
- Exact executor argv tests and preflight decision-matrix tests

<output>After completion, create `05-01-SUMMARY.md`.</output>
