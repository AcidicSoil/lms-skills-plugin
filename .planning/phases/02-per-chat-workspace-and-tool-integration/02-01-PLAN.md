---
phase: 02-per-chat-workspace-and-tool-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [src/constants.ts, src/pluginTypes.ts, src/types.ts, src/workspace.ts, test/workspace.test.ts]
autonomous: true
requirements: [WORK-01, WORK-02, WORK-03, WORK-04, WORK-05]
user_setup: []
must_haves:
  truths:
    - The same LM Studio provider working directory resolves to the same workspace ID and root.
    - Host and WSL workspace identities cannot collide.
    - Workspace creation is idempotent and capability failures do not mutate state.
    - Users can inspect workspace ID, environment, distribution, and native root.
  artifacts:
    - src/workspace.ts
    - test/workspace.test.ts
  key_links:
    - src/pluginTypes.ts exposes the installed SDK's getWorkingDirectory seam.
    - src/workspace.ts consumes effective execution settings and Phase 1 capability/path contracts.
---

<objective>
Create the deterministic workspace identity, root policy, lifecycle, and inspection contract used by every project-scoped tool.

Purpose: Give Host and WSL operations one stable per-chat/project context before migrating tools.
Output: Workspace service, controller seam, constants/types, and tests.
</objective>

<execution_context>
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/workflows/execute-plan.md
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-per-chat-workspace-and-tool-integration/02-CONTEXT.md
@.planning/phases/02-per-chat-workspace-and-tool-integration/02-RESEARCH.md
@.planning/phases/01-execution-and-path-foundation/01-VERIFICATION.md
@src/pluginTypes.ts
@src/settings.ts
@src/wslCapability.ts
@src/pathPolicy.ts
@src/executor.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Test-drive deterministic workspace identity and metadata</name>
  <files>src/pluginTypes.ts, src/types.ts, src/workspace.ts, test/workspace.test.ts</files>
  <read_first>src/pluginTypes.ts, src/types.ts, node_modules/@lmstudio/sdk/dist/index.d.ts, .planning/phases/02-per-chat-workspace-and-tool-integration/02-RESEARCH.md</read_first>
  <action>Extend the local tools-provider controller interface with `getWorkingDirectory(): string`, matching the installed SDK. Define `WorkspaceContext` and a deterministic ID function using a stable cryptographic hash over normalized provider working-directory identity plus execution environment and WSL distribution where applicable. Reject empty identity. Return inspectable metadata without depending on per-tool-call IDs.</action>
  <verify>npm test -- --test-name-pattern="workspace identity"</verify>
  <acceptance_criteria>
    - Same normalized identity/settings produce the same ID.
    - Different identity, environment, or distribution produces a different ID.
    - Empty provider working directory returns a typed error.
    - No tool-call ID is referenced by workspace code.
  </acceptance_criteria>
  <done>WORK-01 and inspection metadata foundations are covered.</done>
</task>

<task type="auto">
  <name>Task 2: Implement idempotent Host and WSL root resolution</name>
  <files>src/constants.ts, src/workspace.ts, test/workspace.test.ts</files>
  <read_first>src/constants.ts, src/wslCapability.ts, src/executor.ts, src/pathPolicy.ts, src/workspace.ts</read_first>
  <action>Add plugin-owned Host and Linux-native WSL workspace root policies. Host creation uses injected Node filesystem functions. WSL creation uses an injected structured execution/capability boundary with explicit distribution and Linux path; do not create WSL roots through UNC or `/mnt/c`. Validate WSL capability before mutation. Make repeated creation idempotent and return structured errors for removed distribution or inaccessible root.</action>
  <verify>npm test -- --test-name-pattern="workspace root" && npm run build</verify>
  <acceptance_criteria>
    - Host root is plugin-owned and native to the host.
    - WSL root is Linux-native and includes the deterministic ID.
    - Repeated resolution performs no destructive reset.
    - Missing distribution fails before workspace creation.
  </acceptance_criteria>
  <done>WORK-02 through WORK-05 are implemented and tested.</done>
</task>
</tasks>

<verification>
- [ ] npm test
- [ ] npm run build
- [ ] Workspace tests require neither Windows nor a live WSL installation.
</verification>

<success_criteria>
- Deterministic workspace context exists for Host and WSL.
- Creation is idempotent.
- Inspection metadata is complete.
- Capability failures are safe and typed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-per-chat-workspace-and-tool-integration/02-01-SUMMARY.md`
</output>
