---
phase: 05-backend-guarantees-and-validation
plan: 03
type: standard
wave: 3
depends_on: [05-01, 05-02]
files_modified: [src/toolsProvider.ts, src/pluginTypes.ts, src/preflight.ts, src/recoveryError.ts, test/toolsProvider.integration.test.ts, test/workspaceConfiguration.integration.test.ts, test/compatibility.test.ts]
autonomous: true
requirements: [BACK-07, BACK-08, TEST-01, TEST-04, TEST-06, TEST-08]
user_setup: []
must_haves:
  truths:
    - Every workspace-facing tool runs preflight before executor, filesystem, or discovery side effects.
    - Host and WSL settings remain isolated across chat controllers.
    - Invalid workspace paths never fall back to home or process cwd.
    - Distribution deletion, misspelling, and default changes return actionable structured errors.
    - Public failures include stable codes, messages, and recovery actions without breaking successful tool contracts.
  artifacts: [test/toolsProvider.integration.test.ts, test/workspaceConfiguration.integration.test.ts]
  key_links:
    - toolsProvider maps preflight failures through recoveryError.ts.
    - all tools use the backend context created after successful preflight.
---

<objective>
Integrate preflight and recovery errors across the public tool surface, then prove Host/WSL isolation and strict failure behavior end to end.
</objective>

<context>
@.planning/phases/05-backend-guarantees-and-validation/05-01-SUMMARY.md
@.planning/phases/05-backend-guarantees-and-validation/05-02-SUMMARY.md
@src/toolsProvider.ts
@src/preflight.ts
@src/recoveryError.ts
@test/toolsProvider.integration.test.ts
@test/workspaceConfiguration.integration.test.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce preflight and Host/WSL isolation across public tools</name>
  <files>src/toolsProvider.ts, src/pluginTypes.ts, src/preflight.ts, test/toolsProvider.integration.test.ts, test/workspaceConfiguration.integration.test.ts</files>
  <read_first>src/toolsProvider.ts, src/preflight.ts, src/backend.ts, test/toolsProvider.integration.test.ts, test/workspaceConfiguration.integration.test.ts</read_first>
  <behavior>
    - Invalid path, unavailable environment, and invalid distribution reject before command/filesystem/discovery spies are called.
    - Separate Host and WSL controllers retain isolated settings, backend, cwd, and distribution state.
    - Missing, renamed, deleted, and changed-default distributions have distinct outcomes.
    - No failure path substitutes home or process cwd.
  </behavior>
  <action>Run the shared preflight at the common tool invocation boundary and construct backend context only on success. Expand integration coverage with side-effect spies, multiple controllers, invalid paths, named/default distribution changes, and WSL capability failures. Preserve Phase 4 controller-scoped caching.</action>
  <acceptance_criteria>
    - Side-effect spies remain at zero for all failed preflight cases.
    - Host and WSL controller tests prove no state leakage.
    - Distribution scenarios have deterministic codes and recovery actions.
    - `npm test -- --test-name-pattern="Host and WSL isolation|no fallback|distribution"` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="Host and WSL isolation|no fallback|distribution"</automated></verify>
  <done>All workspace-facing public tools fail closed before invocation and keep chat environments isolated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize structured public recovery responses</name>
  <files>src/toolsProvider.ts, src/pluginTypes.ts, src/recoveryError.ts, test/toolsProvider.integration.test.ts, test/compatibility.test.ts</files>
  <read_first>src/recoveryError.ts, src/toolsProvider.ts, src/pluginTypes.ts, test/compatibility.test.ts</read_first>
  <behavior>
    - Every BACK-08 failure returns `success: false`, stable `errorCode`, human `message`, and `recoveryActions`.
    - Existing successful response shapes and tool names remain compatible.
    - Capability/status tools expose the same codes used by operational failures.
  </behavior>
  <action>Add one response mapper from `RecoveryError` to public tool results and use it consistently. Extend compatibility tests for intentional structured fields while preserving existing successful contracts. Avoid leaking command output, file contents, or internal stack traces in recovery metadata.</action>
  <acceptance_criteria>
    - All seven recovery categories have integration assertions.
    - Successful v1.0/Phase 4 contracts still pass compatibility tests.
    - `npm test -- --test-name-pattern="structured recovery|capability state|compatibility" && npm run build` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="structured recovery|capability state|compatibility" && npm run build</automated></verify>
  <done>Clients receive consistent actionable errors across the complete tool surface.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 5 regression and release verification</name>
  <files>test/toolsProvider.integration.test.ts, test/compatibility.test.ts</files>
  <read_first>package.json, scripts/verify-release.mjs, .planning/phases/05-backend-guarantees-and-validation/05-VALIDATION.md</read_first>
  <action>Close any missing assertions required by the validation matrix, then run the full project suite, TypeScript build, release verification, and diff checks. Fix only Phase 5 regressions within owned files.</action>
  <acceptance_criteria>
    - All Phase 5 requirement IDs have behavioral test evidence.
    - `npm test`, `npm run build`, `npm run verify:release`, and `git diff --check` all exit 0.
  </acceptance_criteria>
  <verify><automated>npm test && npm run build && npm run verify:release && git diff --check</automated></verify>
  <done>Phase 5 is release-verified with no unresolved backend or validation gaps.</done>
</task>
</tasks>

<verification>
- [ ] Full automated suite passes.
- [ ] Build and release verification pass.
- [ ] No unresolved plan-checker findings remain.
</verification>

## Artifacts this phase produces

- Common preflight enforcement in `src/toolsProvider.ts`
- Stable public recovery response types
- Host/WSL isolation, no-fallback, distribution-change, and structured-error integration tests

<output>After completion, create `05-03-SUMMARY.md`.</output>
