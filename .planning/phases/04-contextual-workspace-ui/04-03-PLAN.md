---
phase: 04-contextual-workspace-ui
plan: 03
type: standard
wave: 3
depends_on: [04-01, 04-02]
files_modified: [src/toolsProvider.ts, src/pluginTypes.ts, src/index.ts, test/toolsProvider.integration.test.ts]
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-06, UI-07, UI-08]
user_setup: []
must_haves:
  truths:
    - Consumers can retrieve the active workspace profile, environment, native path, and explicit status.
    - WSL capability state can be refreshed and returned without mutating the active selection.
    - Invalid workspace state blocks workspace-facing operations and never falls back home.
    - Status output distinguishes chat-scoped selection from persistent defaults.
  artifacts:
    - src/toolsProvider.ts
    - test/toolsProvider.integration.test.ts
  key_links:
    - The public status surface consumes `workspaceSelection.ts` and `wslCapability.ts` rather than reimplementing their logic.
    - Workspace-facing tools use the same selected context reported by the status surface.
---

<objective>
Expose contextual workspace and WSL capability status through the plugin runtime and prove end-to-end behavior.

Purpose: Turn the Phase 4 data and capability contracts into an observable surface that the plugin UI/model can use consistently.
Output: Runtime status/refresh surface and integration tests for scope, validity, and no-fallback guarantees.
</objective>

<execution_context>
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/workflows/execute-plan.md
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-contextual-workspace-ui/CONTEXT.md
@.planning/phases/04-contextual-workspace-ui/04-RESEARCH.md
@.planning/phases/04-contextual-workspace-ui/04-01-SUMMARY.md
@.planning/phases/04-contextual-workspace-ui/04-02-SUMMARY.md
@src/toolsProvider.ts
@src/pluginTypes.ts
@src/index.ts
@test/toolsProvider.integration.test.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add integration coverage for contextual status and strict invalid-state behavior</name>
  <files>test/toolsProvider.integration.test.ts</files>
  <read_first>test/toolsProvider.integration.test.ts, src/toolsProvider.ts, src/workspace.ts, src/workspaceSelection.ts, src/wslCapability.ts</read_first>
  <behavior>
    - Status reports active profile name, chat scope, environment, native path, and validity code.
    - Unset and invalid selections return explicit status without a synthesized cwd.
    - WSL capability refresh returns the latest probe result and preserves the selected profile/distribution.
    - A workspace-facing command issued while status is invalid fails before execution.
  </behavior>
  <action>Add failing integration tests around the real tools provider lifecycle. Cover Host and WSL status payloads, chat-scoped versus persistent/default metadata, all workspace validity categories that can be simulated, capability refresh, and proof that invalid state blocks operations without invoking a home-directory fallback. Prefer stable fields and error codes over full-object snapshots.</action>
  <acceptance_criteria>
    - Integration tests assert status payload fields for profile ID/name, scope, environment, native path, workspace status code, and WSL capability code.
    - Unset and invalid selections produce no executable cwd and no home-directory substitution.
    - Capability refresh preserves persisted profile and distribution selection.
    - Invalid workspace state prevents the command executor mock from being called.
    - `npm test -- --test-name-pattern="workspace status|capability refresh|no fallback"` exits 0 after implementation.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="workspace status|capability refresh|no fallback"</automated></verify>
  <done>Integration tests specify the complete user-visible Phase 4 status contract before runtime wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Wire status and refresh into the plugin runtime</name>
  <files>src/toolsProvider.ts, src/pluginTypes.ts, src/index.ts</files>
  <read_first>src/toolsProvider.ts, src/pluginTypes.ts, src/index.ts, src/workspaceSelection.ts, src/wslCapability.ts, test/toolsProvider.integration.test.ts</read_first>
  <action>Add or extend the narrowest public workspace-facing status surface so callers can retrieve active profile identity, scope, environment, native path, workspace validity, and WSL capability. Add a refresh operation that performs a new capability probe without modifying persisted selection. Reuse the Phase 4 pure status and capability modules; do not duplicate validation logic in the tool layer. Ensure workspace-facing operations consult the same resolved selection and fail with actionable structured output when it is not valid.</action>
  <acceptance_criteria>
    - `src/toolsProvider.ts` exposes a status tool returning the exact fields covered by the integration tests.
    - A separate refresh operation invokes the WSL capability probe and does not write settings.
    - Workspace-facing operations consume the same resolved selection/status object and reject non-`valid` state with a stable error code.
    - `npm test -- --test-name-pattern="workspace status|capability refresh|no fallback|tools provider" && npm run build` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="workspace status|capability refresh|no fallback|tools provider" && npm run build</automated></verify>
  <done>The runtime exposes one coherent contextual status contract and enforces it before workspace operations.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 4 regression and release verification</name>
  <files>test/toolsProvider.integration.test.ts</files>
  <read_first>package.json, scripts/verify-release.mjs, test/compatibility.test.ts, test/diagnostics.test.ts</read_first>
  <action>Complete any missing integration assertions needed to prove Host and WSL settings remain isolated, legacy Host behavior remains compatible, and new status fields do not break existing tool contracts. Run the full project verification suite and fix Phase 4 regressions within the files owned by this phase.</action>
  <acceptance_criteria>
    - Host and WSL settings remain isolated in integration coverage.
    - Existing v1.0 tool names and successful Host behavior remain compatible unless explicitly extended by this phase.
    - `npm test`, `npm run build`, `npm run verify:release`, and `git diff --check` all exit 0.
  </acceptance_criteria>
  <verify><automated>npm test && npm run build && npm run verify:release && git diff --check</automated></verify>
  <done>Phase 4 passes all focused, full-suite, build, and release verification gates.</done>
</task>
</tasks>

<verification>
- [ ] npm test
- [ ] npm run build
- [ ] npm run verify:release
- [ ] git diff --check
</verification>

<success_criteria>
- The contextual workspace status is observable end to end.
- Invalid workspace state blocks execution without fallback.
- Capability refresh is explicit and side-effect free.
- Existing public tool behavior remains compatible.
</success_criteria>

## Artifacts this phase produces

- Contextual workspace status tool in `src/toolsProvider.ts`
- WSL capability refresh tool/operation in `src/toolsProvider.ts`
- Public status payload types in `src/pluginTypes.ts`
- Runtime registration/wiring in `src/index.ts`
- Expanded integration assertions in `test/toolsProvider.integration.test.ts`

<output>
After completion, create `.planning/phases/04-contextual-workspace-ui/04-03-SUMMARY.md`
</output>
