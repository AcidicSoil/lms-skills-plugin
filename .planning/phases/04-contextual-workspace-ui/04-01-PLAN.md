---
phase: 04-contextual-workspace-ui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [src/types.ts, src/settings.ts, src/workspaceSelection.ts, test/settings.test.ts, test/workspaceSelection.test.ts]
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-08]
user_setup: []
must_haves:
  truths:
    - Active workspace selection has an explicit profile identity, environment, path, and validity state.
    - Unset, valid, unavailable, moved, and configuration-required states are distinguishable.
    - Invalid or unavailable paths never resolve to the home directory.
    - Chat-scoped active selection, persistent profiles, and global defaults remain separate concepts.
  artifacts:
    - src/workspaceSelection.ts
    - test/workspaceSelection.test.ts
  key_links:
    - Settings normalization feeds the pure workspace-selection status model.
    - Workspace status is derived without performing Host filesystem checks on WSL paths.
---

<objective>
Create the typed workspace-selection and status foundation for Phase 4.

Purpose: Give later UI and tool surfaces one explicit contract for active profile identity, environment-specific paths, validity, and scope.
Output: Backward-compatible settings types, a pure status derivation module, and focused unit coverage.
</objective>

<execution_context>
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/workflows/execute-plan.md
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-contextual-workspace-ui/CONTEXT.md
@.planning/phases/04-contextual-workspace-ui/04-RESEARCH.md
@src/types.ts
@src/settings.ts
@src/workspace.ts
@test/settings.test.ts
@test/workspace.test.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define workspace profile, active selection, and status contracts</name>
  <files>src/types.ts, src/workspaceSelection.ts, test/workspaceSelection.test.ts</files>
  <read_first>src/types.ts, src/workspace.ts, src/wslCapability.ts, test/workspace.test.ts</read_first>
  <behavior>
    - Unset selection returns an `unset` state without synthesizing a path.
    - Valid Host and WSL selections retain their environment-native paths.
    - Missing or inaccessible paths return `unavailable` or `configuration-required` rather than a fallback path.
    - A profile path mismatch can be represented as `moved` without changing the persisted profile automatically.
  </behavior>
  <action>Create discriminated TypeScript types for durable workspace profiles, chat-scoped active selection, and user-visible workspace status. Implement a pure derivation function in `src/workspaceSelection.ts` that consumes profile/selection data plus backend-provided validation facts. Keep status derivation free of direct filesystem calls so Host and WSL validation remain backend-specific. Model global defaults separately from active selection and profile records.</action>
  <acceptance_criteria>
    - `src/workspaceSelection.ts` exports discriminated profile, active-selection, and workspace-status types plus a pure status derivation function.
    - `test/workspaceSelection.test.ts` asserts `unset`, `valid`, `unavailable`, `moved`, and `configuration-required` states.
    - No branch in the derivation function imports Node filesystem APIs or substitutes a home-directory path.
    - `npm test -- --test-name-pattern="workspace selection"` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="workspace selection"</automated></verify>
  <done>The project has one deterministic, unit-tested status contract covering all Phase 4 workspace states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend persisted settings without breaking v1.0 users</name>
  <files>src/settings.ts, src/types.ts, test/settings.test.ts</files>
  <read_first>src/settings.ts, src/types.ts, test/settings.test.ts, test/compatibility.test.ts</read_first>
  <behavior>
    - Legacy settings with no profile fields normalize successfully.
    - Host and WSL workspace defaults remain distinct.
    - Empty WSL distribution remains the canonical system-default selection.
    - No normalization path inserts a home-directory workspace for missing or invalid data.
  </behavior>
  <action>Add the smallest persisted settings shape needed for global workspace defaults and durable profile references while preserving all existing keys and defaults. Update normalization, load, save, and effective-config resolution together. Treat malformed optional profile data as recoverable configuration-required state rather than silently replacing it with a home path. Preserve compatibility with v1.0 settings files.</action>
  <acceptance_criteria>
    - `src/settings.ts` normalizes legacy v1.0 settings with no workspace profile fields.
    - Empty `wslDistribution` remains unchanged and means system default.
    - Host and WSL workspace defaults are stored in separate fields.
    - Settings normalization never calls `os.homedir()` to replace missing or invalid workspace data.
    - `npm test -- --test-name-pattern="settings|compatibility" && npm run build` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="settings|compatibility" && npm run build</automated></verify>
  <done>Legacy and new settings normalize deterministically, with separate scope concepts and no silent workspace fallback.</done>
</task>
</tasks>

<verification>
- [ ] npm test -- --test-name-pattern="workspace selection|settings|compatibility"
- [ ] npm run build
- [ ] git diff --check
</verification>

<success_criteria>
- UI-01, UI-02, UI-03, and UI-08 have an implementation-ready data contract.
- All five workspace states are directly testable.
- Legacy settings continue to work unchanged.
</success_criteria>

## Artifacts this phase produces

- `src/workspaceSelection.ts`
- Workspace profile, active-selection, validation-fact, and workspace-status TypeScript types
- Pure workspace-status derivation function
- New persisted workspace-default/profile-reference fields in `src/types.ts` and `src/settings.ts`
- `test/workspaceSelection.test.ts`

<output>
After completion, create `.planning/phases/04-contextual-workspace-ui/04-01-SUMMARY.md`
</output>
