import test from "node:test"; import assert from "node:assert/strict"; import { WorkspaceInvocationRegistry } from "../src/invocationRegistry";
test("invocation lock tracks ownership and release",()=>{const r=new WorkspaceInvocationRegistry();const release=r.acquire({chatId:"c",profileId:"p",workspaceId:"w"});assert.deepEqual(r.canMutate("w"),{ok:false,reason:"active"});release();assert.deepEqual(r.canMutate("w"),{ok:true});});
test("termination unresolved survives release until resolved",()=>{const r=new WorkspaceInvocationRegistry();const release=r.acquire({chatId:"c",workspaceId:"w"});r.markTerminationUnresolved("w");release();assert.deepEqual(r.canMutate("w"),{ok:false,reason:"termination-unresolved"});r.resolveTermination("w");assert.deepEqual(r.canMutate("w"),{ok:true});});
