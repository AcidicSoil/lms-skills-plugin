---
phase: 02-per-chat-workspace-and-tool-integration
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified: [src/executor.ts, src/workspaceFs.ts, test/workspaceFs.test.ts]
autonomous: true
requirements: [TOOL-01]
user_setup: []
must_haves:
  truths:
    - Every project file operation resolves through one workspace context and rejects escape.
    - Host and WSL backends share one typed operation contract.
    - WSL file content is never interpolated into a shell command.
  artifacts:
    - src/workspaceFs.ts
    - test/workspaceFs.test.ts
  key_links:
    - src/workspaceFs.ts consumes WorkspaceContext from Plan 01 and pathPolicy containment.
    - WSL backend uses direct argv/stdin-safe execution rather than shell-string file mutation.
---

<objective>
Build the workspace-scoped filesystem service for Host and WSL with containment and structured operation contracts.

Purpose: Remove direct arbitrary filesystem access from project tools before provider migration.
Output: Workspace filesystem abstraction, WSL direct-execution support, and backend tests.
</objective>

<execution_context>
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/workflows/execute-plan.md
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-per-chat-workspace-and-tool-integration/02-01-SUMMARY.md
@.planning/phases/02-per-chat-workspace-and-tool-integration/02-RESEARCH.md
@src/workspace.ts
@src/pathPolicy.ts
@src/executor.ts
@src/toolsProvider.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define and test workspace path resolution and Host filesystem backend</name>
  <files>src/workspaceFs.ts, test/workspaceFs.test.ts</files>
  <read_first>src/workspace.ts, src/pathPolicy.ts, src/toolsProvider.ts</read_first>
  <action>Create a typed service covering read, write, patch, append, mkdir, list, delete, move, and rename. Resolve relative paths from `WorkspaceContext.nativeRoot`; permit absolute paths only when valid for the environment and canonically contained. Host operations use injected filesystem functions. Preserve response metadata such as resolved path, bytes, and directory entries.</action>
  <verify>npm test -- --test-name-pattern="host workspace filesystem"</verify>
  <acceptance_criteria>
    - All operations use the same context root.
    - Traversal, sibling-prefix, and canonical link escape fail before mutation.
    - Host create/write/read/patch/list/move/delete lifecycle passes in a temporary root.
  </acceptance_criteria>
  <done>Host half of TOOL-01 is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Add structured WSL filesystem backend</name>
  <files>src/executor.ts, src/workspaceFs.ts, test/workspaceFs.test.ts</files>
  <read_first>src/executor.ts, src/workspaceFs.ts, .planning/phases/02-per-chat-workspace-and-tool-integration/02-CONTEXT.md</read_first>
  <action>Add a direct program-plus-argv execution primitive with optional stdin payload, preserving Phase 1 timeout/output behavior. Implement WSL file operations using explicit distribution, Linux-native cwd/path, direct utilities or a compact helper invoked without interpolating content into command strings. Test exact argv and stdin boundaries for spaces, quotes, Unicode, and multiline content. Do not use UNC-host filesystem mutation.</action>
  <verify>npm test -- --test-name-pattern="wsl workspace filesystem" && npm run build</verify>
  <acceptance_criteria>
    - File content travels through stdin or another non-interpolated payload channel.
    - WSL program and argv are asserted exactly in tests.
    - Wrong-environment paths and escape attempts fail closed.
    - WSL backend requires no live distribution in tests.
  </acceptance_criteria>
  <done>WSL half of TOOL-01 is complete.</done>
</task>
</tasks>

<verification>
- [ ] npm test
- [ ] npm run build
- [ ] No workspace file content appears in constructed shell command strings.
</verification>

<success_criteria>
- One filesystem service supports every project-scoped operation.
- Host and WSL semantics are covered by tests.
- Workspace containment is mandatory for all mutations and reads.
</success_criteria>

<output>
After completion, create `.planning/phases/02-per-chat-workspace-and-tool-integration/02-02-SUMMARY.md`
</output>
