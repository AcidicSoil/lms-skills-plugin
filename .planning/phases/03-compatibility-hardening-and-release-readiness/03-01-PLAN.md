---
phase: 03-compatibility-hardening-and-release-readiness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [package.json, scripts/verify-release.mjs, test/compatibility.test.ts, test/diagnostics.test.ts]
autonomous: true
requirements: [TEST-03]
user_setup: []
must_haves:
  truths:
    - Legacy Host workflows and model-facing tool names remain compatible.
    - Unsupported WSL, removed distributions, inaccessible roots, and timeout uncertainty surface actionable diagnostics.
    - A clean release verification command rebuilds ignored dist artifacts and checks required outputs.
  artifacts:
    - test/compatibility.test.ts
    - test/diagnostics.test.ts
    - scripts/verify-release.mjs
  key_links:
    - Compatibility tests invoke the actual toolsProvider and settings normalization seams.
    - Diagnostics tests propagate failures through workspace/tool boundaries, not only helper units.
    - package.json exposes the release verification script.
---

<objective>
Harden Host compatibility and user-facing diagnostics, then add a deterministic clean release verification command.

Purpose: Prove existing users can adopt Host/WSL workspaces without regressions and that release builds are reproducible.
Output: Compatibility tests, diagnostic tests, release verification script, and package command.
</objective>

<execution_context>
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/workflows/execute-plan.md
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-compatibility-hardening-and-release-readiness/03-CONTEXT.md
@.planning/phases/03-compatibility-hardening-and-release-readiness/03-RESEARCH.md
@.planning/phases/01-execution-and-path-foundation/01-VERIFICATION.md
@.planning/phases/02-per-chat-workspace-and-tool-integration/02-VERIFICATION.md
@package.json
@src/settings.ts
@src/toolsProvider.ts
@src/workspace.ts
@src/executor.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add RED compatibility coverage for legacy Host behavior and tool contracts</name>
  <files>test/compatibility.test.ts</files>
  <read_first>test/settings.test.ts, test/toolsProvider.integration.test.ts, src/settings.ts, src/toolsProvider.ts</read_first>
  <action>Write failing tests that assert missing legacy execution settings normalize to Host, the complete public tool-name set remains available, required response fields remain present, skill tools do not resolve project workspaces, and a Host create/edit/read/run/delete flow succeeds from one workspace. Assert missing/invalid command cwd fails explicitly rather than falling back home. Commit RED before implementation changes.</action>
  <verify>npm test -- --test-name-pattern="compatibility"</verify>
  <acceptance_criteria>
    - Tests cover all existing public tool names without asserting fragile full-object equality.
    - Host file and command operations share the same workspace.
    - Legacy settings require no migration action.
    - Invalid cwd never resolves to home.
  </acceptance_criteria>
  <done>Host compatibility regression suite exists and initially fails for any uncovered contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add diagnostic propagation tests and implement any missing actionable errors</name>
  <files>test/diagnostics.test.ts, src/workspace.ts, src/toolsProvider.ts, src/executor.ts</files>
  <read_first>src/wslCapability.ts, src/workspace.ts, src/toolsProvider.ts, src/executor.ts, test/workspace.test.ts, test/executor.test.ts</read_first>
  <action>Test user-facing propagation for unsupported WSL, unavailable `wsl.exe`, no distributions, removed selected distribution, inaccessible Host root, inaccessible WSL home/root creation, canonical escape, existing destination collision, and timeout termination uncertainty. Add or refine structured messages/results only where tests reveal a gap. Preserve no-silent-fallback behavior and existing response shapes.</action>
  <verify>npm test -- --test-name-pattern="diagnostic|compatibility" && npm run build</verify>
  <acceptance_criteria>
    - Every roadmap diagnostic scenario has a behavioral assertion.
    - Errors name the failing capability/path/action and suggest no silent recovery.
    - Timeout output exposes `timedOut` plus termination uncertainty when applicable.
    - Existing Phase 1 and Phase 2 tests remain green.
  </acceptance_criteria>
  <done>Compatibility and diagnostic coverage satisfies TEST-03 behaviorally.</done>
</task>

<task type="auto">
  <name>Task 3: Add clean release-build verification</name>
  <files>package.json, scripts/verify-release.mjs</files>
  <read_first>package.json, tsconfig.json, .gitignore</read_first>
  <action>Create a cross-platform Node release script that removes `dist` and `.test-dist`, runs the full test suite and TypeScript build, checks required generated entry/module/declaration files, runs `git diff --check`, and fails if tracked source files changed. Add `npm run verify:release`. Do not require ignored `dist/` files to be committed.</action>
  <verify>npm run verify:release</verify>
  <acceptance_criteria>
    - Verification starts from clean generated directories.
    - `dist/index.js`, declarations, workspace modules, and executor outputs are checked.
    - Script exits nonzero on missing artifacts or tracked build drift.
    - Command works without Bash-only package-script syntax.
  </acceptance_criteria>
  <done>Release build consistency is reproducibly verifiable.</done>
</task>
</tasks>

<verification>
- [ ] npm test
- [ ] npm run build
- [ ] npm run verify:release
- [ ] git diff --check
</verification>

<success_criteria>
- Existing Host behavior is protected by regression tests.
- All required diagnostic failures are actionable.
- Clean generated artifacts are reproducible and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-compatibility-hardening-and-release-readiness/03-01-SUMMARY.md`
</output>
