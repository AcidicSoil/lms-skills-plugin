---
phase: 07-compatibility-and-advanced-management
plan: 02
type: tdd
wave: 2
depends_on: [07-01]
files_modified: [src/types.ts, src/approvalHistory.ts, src/settings.ts, src/toolsProvider.ts, test/approvalHistory.test.ts, test/toolsProvider.integration.test.ts]
autonomous: true
requirements: [COMP-03, COMP-04]
user_setup: []
must_haves:
  truths:
    - Approval history is scoped by workspace/profile and bounded by count and age.
    - Records never contain command output, file contents, transcripts, or full command strings.
    - Users can list, clear, and revoke workspace-specific approval records.
    - Expired or revoked approvals cannot authorize later operations.
  artifacts: [src/approvalHistory.ts, test/approvalHistory.test.ts]
  key_links:
    - approval decisions append redacted records through one store.
    - toolsProvider exposes management operations without bypassing retention/redaction.
---

<objective>
Add bounded, redacted, workspace-specific approval history and management controls.
</objective>

<context>
@.planning/phases/07-compatibility-and-advanced-management/07-01-SUMMARY.md
@.planning/phases/07-compatibility-and-advanced-management/07-RESEARCH.md
@src/toolMetadata.ts
@src/preflight.ts
@src/settings.ts
@src/toolsProvider.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the bounded redacted approval-history store</name>
  <files>src/types.ts, src/approvalHistory.ts, src/settings.ts, test/approvalHistory.test.ts</files>
  <read_first>src/toolMetadata.ts, src/preflight.ts, src/settings.ts, src/types.ts</read_first>
  <behavior>
    - Records contain workspace/profile ID, tool name, normalized path scope, read/write scope, decision, timestamp, and expiry only.
    - Appending enforces maximum count and maximum age per workspace.
    - Raw commands, outputs, file contents, and transcripts are rejected or omitted.
    - Revoked and expired records never match authorization queries.
  </behavior>
  <action>Create a pure `ApprovalHistoryStore` contract and persisted record schema. Implement append, list/filter, find-active-grant, revoke, clear-workspace, and retention pruning. Redact inputs at construction time and make sensitive fields impossible in the public record type.</action>
  <acceptance_criteria>
    - Tests prove count and age retention boundaries.
    - Serialized records contain none of the forbidden sensitive fields or raw command text.
    - Revoked/expired records are excluded from active-grant lookup.
    - `npm test -- --test-name-pattern="approval history|redaction|retention"` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="approval history|redaction|retention"</automated></verify>
  <done>Approval history is useful for management without becoming a content or transcript log.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose workspace approval-history management tools</name>
  <files>src/toolsProvider.ts, src/approvalHistory.ts, src/settings.ts, test/toolsProvider.integration.test.ts</files>
  <read_first>src/toolsProvider.ts, src/approvalHistory.ts, src/settings.ts, test/toolsProvider.integration.test.ts</read_first>
  <behavior>
    - Users can list bounded history for one workspace.
    - Users can revoke one record or clear a workspace's history.
    - Management actions cannot expose command output, content, or full command text.
    - Persisted changes survive provider recreation.
  </behavior>
  <action>Add `list_workspace_approvals`, `revoke_workspace_approval`, and `clear_workspace_approvals` tools backed by the store. Return compact safe metadata and structured errors for unknown workspace/record IDs. Update compatibility coverage for intentional tool additions.</action>
  <acceptance_criteria>
    - Tool integration tests cover list, revoke, clear, persistence, and safe response shape.
    - Responses contain no forbidden sensitive fields.
    - `npm test -- --test-name-pattern="approval history tools|workspace history|compatibility" && npm run build` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="approval history tools|workspace history|compatibility" && npm run build</automated></verify>
  <done>Workspace approval history is visible and controllable through safe runtime tools.</done>
</task>
</tasks>

<verification>
- [ ] Approval store and management tests pass.
- [ ] `npm run build` passes.
- [ ] `git diff --check` passes.
</verification>

## Artifacts this phase produces

- `src/approvalHistory.ts`
- Persisted bounded/redacted approval records
- Approval-history list/revoke/clear tools and integration tests

<output>After completion, create `07-02-SUMMARY.md`.</output>
