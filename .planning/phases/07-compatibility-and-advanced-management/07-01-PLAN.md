---
phase: 07-compatibility-and-advanced-management
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [src/types.ts, src/settings.ts, src/settingsMigration.ts, test/settings.test.ts, test/settingsMigration.test.ts, test/compatibility.test.ts]
autonomous: true
requirements: [COMP-01, COMP-02, TEST-07]
user_setup: []
must_haves:
  truths:
    - Persisted settings have an explicit current schema version and migrate stepwise from legacy/unversioned data.
    - Recoverable fields survive malformed or partially valid persisted objects.
    - Unsupported future-version settings are not destructively rewritten.
    - WSL-specific fields remain preserved at rest but cannot become executable WSL state on non-Windows hosts.
  artifacts: [src/settingsMigration.ts, test/settingsMigration.test.ts]
  key_links:
    - settings.ts delegates schema detection and migration to settingsMigration.ts before normalization.
    - compatibility tests cover old, current, future, malformed, and non-Windows cases.
---

<objective>
Add versioned settings migration, recoverable malformed-data handling, downgrade safety, and safe non-Windows behavior.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-compatibility-and-advanced-management/CONTEXT.md
@.planning/phases/07-compatibility-and-advanced-management/07-RESEARCH.md
@src/settings.ts
@src/types.ts
@test/settings.test.ts
@test/compatibility.test.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add explicit settings schema migration and recovery diagnostics</name>
  <files>src/types.ts, src/settings.ts, src/settingsMigration.ts, test/settingsMigration.test.ts, test/settings.test.ts</files>
  <read_first>src/settings.ts, src/types.ts, test/settings.test.ts, .planning/phases/07-compatibility-and-advanced-management/07-RESEARCH.md</read_first>
  <behavior>
    - Unversioned v1.0/v1.1-compatible objects migrate to the current schema without losing valid fields.
    - Partially malformed arrays/fields preserve valid siblings and emit structured diagnostics.
    - Invalid JSON returns a visible load diagnostic and safe defaults without overwriting the source automatically.
    - Migration is deterministic and idempotent.
  </behavior>
  <action>Create a pure migration pipeline with a current schema version, stepwise migration functions, structured diagnostics, and a result envelope containing normalized candidate data plus rewrite safety. Integrate it into settings load/save so migration is committed only after successful validation. Preserve recoverable fields instead of replacing the entire object with defaults.</action>
  <acceptance_criteria>
    - Tests cover unversioned legacy, current, partially malformed, invalid JSON, and idempotent migration.
    - Invalid JSON is not silently overwritten during load.
    - Recoverable workspace profiles and settings survive partial corruption.
    - `npm test -- --test-name-pattern="settings migration|malformed settings|settings"` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="settings migration|malformed settings|settings"</automated></verify>
  <done>Settings migration is explicit, recoverable, and observable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add downgrade-safe future-version and non-Windows handling</name>
  <files>src/settingsMigration.ts, src/settings.ts, src/types.ts, test/settingsMigration.test.ts, test/compatibility.test.ts</files>
  <read_first>src/settingsMigration.ts, src/settings.ts, src/wslCapability.ts, test/compatibility.test.ts</read_first>
  <behavior>
    - Future-version settings load in read-preserving safe mode and are not rewritten by older code.
    - Non-Windows startup preserves WSL profile/distribution fields but forces effective execution away from unavailable WSL.
    - Returning to Windows retains the preserved WSL configuration.
  </behavior>
  <action>Add future-version detection and a no-rewrite downgrade-safe envelope. Add a platform-safety projection for effective config that preserves persisted WSL values while returning Host-safe execution and actionable diagnostics on non-Windows platforms. Do not delete WSL paths or distribution overrides.</action>
  <acceptance_criteria>
    - Future-version files remain byte-for-byte untouched unless the user explicitly saves compatible data.
    - Non-Windows startup never attempts `wsl.exe` and retains stored WSL fields.
    - `npm test -- --test-name-pattern="downgrade|non-Windows startup|WSL settings|compatibility" && npm run build` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="downgrade|non-Windows startup|WSL settings|compatibility" && npm run build</automated></verify>
  <done>Settings remain safe across downgrade and unsupported host platforms.</done>
</task>
</tasks>

<verification>
- [ ] Migration and compatibility tests pass.
- [ ] `npm run build` passes.
- [ ] `git diff --check` passes.
</verification>

## Artifacts this phase produces

- `src/settingsMigration.ts`
- Versioned settings and load-diagnostic contracts
- Migration, malformed-data, future-version, downgrade, and non-Windows tests

<output>After completion, create `07-01-SUMMARY.md`.</output>
