---
phase: 06-workspace-profiles-and-safety
plan: 02
type: tdd
wave: 2
depends_on: [06-01]
files_modified: [src/invocationRegistry.ts, src/toolMetadata.ts, src/preflight.ts, src/recoveryError.ts, src/backend.ts, src/toolsProvider.ts, test/invocationRegistry.test.ts, test/toolMetadata.test.ts]
autonomous: true
requirements: [PROF-02, PROF-03, PROF-04, PROF-05, PROF-06, TEST-05]
user_setup: []
must_haves:
  truths:
    - Active invocations and unresolved termination block workspace switching and deletion.
    - Workspace ownership is tracked by chat and profile.
    - Tool metadata declares workspace requirement, destructiveness, and environment compatibility.
    - Outside-workspace access is path- and scope-specific and requires explicit approval.
    - Destructive tools require confirmation and affected-path previews before invocation.
  artifacts: [src/invocationRegistry.ts, src/toolMetadata.ts, test/invocationRegistry.test.ts]
  key_links:
    - backend/tool execution acquires and releases registry ownership around operations.
    - preflight consumes tool metadata, approvals, identity, and registry state before side effects.
---

<objective>
Add invocation ownership, process-safety locks, tool metadata, scoped approvals, and destructive confirmation contracts.
</objective>

<context>
@.planning/phases/06-workspace-profiles-and-safety/06-01-SUMMARY.md
@.planning/phases/06-workspace-profiles-and-safety/06-RESEARCH.md
@src/backend.ts
@src/executor.ts
@src/preflight.ts
@src/recoveryError.ts
@src/toolsProvider.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Track invocation ownership and unresolved termination</name>
  <files>src/invocationRegistry.ts, src/backend.ts, src/toolsProvider.ts, test/invocationRegistry.test.ts</files>
  <read_first>src/backend.ts, src/executor.ts, src/toolsProvider.ts, src/preflight.ts, test/diagnostics.test.ts</read_first>
  <behavior>
    - Acquiring an invocation records chat ID, profile ID, workspace ID, and active count.
    - Normal completion releases ownership.
    - `terminationIncomplete` keeps the workspace locked until explicitly resolved.
    - Switching or deleting a locked workspace returns a structured busy/termination failure.
  </behavior>
  <action>Implement a deterministic `WorkspaceInvocationRegistry` with acquire, release, markTerminationUnresolved, resolveTermination, and guard methods. Integrate ownership around operational backend calls. Ensure failure paths release normal locks but preserve unresolved termination state. Avoid global cross-project leakage by scoping registry ownership to the provider/plugin instance.</action>
  <acceptance_criteria>
    - Concurrent operations increment/decrement ownership correctly.
    - Switch/delete guards fail during active work and unresolved termination.
    - A resolved termination clears the lock deterministically.
    - `npm test -- --test-name-pattern="invocation lock|termination unresolved|workspace busy"` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="invocation lock|termination unresolved|workspace busy"</automated></verify>
  <done>Workspace lifecycle operations cannot race active or incompletely terminated processes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tool metadata, scoped approvals, and destructive confirmation</name>
  <files>src/toolMetadata.ts, src/preflight.ts, src/recoveryError.ts, src/toolsProvider.ts, test/toolMetadata.test.ts</files>
  <read_first>src/preflight.ts, src/recoveryError.ts, src/toolsProvider.ts, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Metadata declares whether a tool needs a workspace, supported environments, and destructiveness.
    - Outside-workspace grants are limited to explicit normalized paths and read/write scope.
    - Destructive operations require confirmation and expose affected-path previews.
    - Missing approval or confirmation fails before filesystem/command side effects.
  </behavior>
  <action>Create a typed tool metadata registry and approval contract. Extend preflight input to validate workspace requirement, environment compatibility, path-scoped grants, destructive confirmation, and affected-path previews. Map failures through the existing structured recovery response without exposing command output or file contents.</action>
  <acceptance_criteria>
    - All public workspace-facing tools have metadata entries.
    - Tests prove grants cannot be reused for different paths or broader write scope.
    - Destructive operations fail without confirmation and include affected paths in safe metadata.
    - `npm test -- --test-name-pattern="tool metadata|workspace approval|destructive confirmation" && npm run build` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="tool metadata|workspace approval|destructive confirmation" && npm run build</automated></verify>
  <done>Permissions and destructive behavior are explicit, scoped, and enforced before invocation.</done>
</task>
</tasks>

<verification>
- [ ] Invocation and metadata tests pass.
- [ ] `npm run build` passes.
- [ ] `git diff --check` passes.
</verification>

## Artifacts this phase produces

- `src/invocationRegistry.ts`
- `src/toolMetadata.ts`
- Extended preflight approval/safety contracts
- Invocation locking and permission tests

<output>After completion, create `06-02-SUMMARY.md`.</output>
