---
phase: 06-workspace-profiles-and-safety
plan: 03
type: standard
wave: 3
depends_on: [06-01, 06-02]
files_modified: [src/toolsProvider.ts, src/pluginTypes.ts, src/settings.ts, src/workspaceCatalog.ts, src/invocationRegistry.ts, test/workspaceConfiguration.integration.test.ts, test/toolsProvider.integration.test.ts, test/compatibility.test.ts]
autonomous: true
requirements: [PROF-07, PROF-08, PROF-09, PROF-10, PROF-11, TEST-09]
user_setup: []
must_haves:
  truths:
    - Users can browse, search, and incrementally load large workspace lists.
    - Users can add, rename, locate, trust, prefer, enable/disable, delete, restore, and permanently delete profiles through explicit tools.
    - Per-chat switching is transactional and does not become active until all safety guards pass.
    - Disabled, deleted, untrusted, mismatched, active, or termination-locked profiles cannot become active silently.
    - Successful switching invalidates cached backend state and subsequent operations use the new workspace.
  artifacts: [test/workspaceConfiguration.integration.test.ts, test/toolsProvider.integration.test.ts]
  key_links:
    - picker tools delegate to workspaceCatalog instead of editing persisted arrays directly.
    - switch_workspace composes catalog, identity, trust, invocation registry, settings persistence, and backend cache invalidation.
---

<objective>
Expose the searchable/infinite workspace picker and lifecycle controls, then prove safe seamless per-chat switching end to end.
</objective>

<context>
@.planning/phases/06-workspace-profiles-and-safety/06-01-SUMMARY.md
@.planning/phases/06-workspace-profiles-and-safety/06-02-SUMMARY.md
@.planning/phases/06-workspace-profiles-and-safety/06-RESEARCH.md
@src/toolsProvider.ts
@src/workspaceCatalog.ts
@src/invocationRegistry.ts
@src/settings.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add paginated workspace picker and profile management tools</name>
  <files>src/toolsProvider.ts, src/pluginTypes.ts, src/settings.ts, src/workspaceCatalog.ts, test/workspaceConfiguration.integration.test.ts</files>
  <read_first>src/toolsProvider.ts, src/workspaceCatalog.ts, src/settings.ts, test/workspaceConfiguration.integration.test.ts</read_first>
  <behavior>
    - `list_workspaces` supports query, cursor, and bounded limit, returning `items` and `nextCursor`.
    - Large lists load incrementally without duplicate profiles.
    - Add/update actions expose name, Host/WSL paths, enabled, trusted, and preferred fields.
    - Global workspace enable/disable state is visible and persisted.
    - Soft delete, restore, and permanent delete have explicit separate actions.
  </behavior>
  <action>Expose a contextual workspace picker API through tools backed by `WorkspaceCatalog`: list/search/page, add, update, set enabled/trusted/preferred, soft delete, restore, permanent delete, and global enable/disable. Keep payloads compact and stable for an infinite-scroll UI. Persist through the existing settings update path and return updated profile/status metadata.</action>
  <acceptance_criteria>
    - Integration tests page through at least 500 profiles with no duplicates and deterministic preferred ordering.
    - Search and add-workspace input behavior are covered.
    - Trusted, preferred, enabled, deleted, and restored states round-trip through tools and settings.
    - `npm test -- --test-name-pattern="workspace picker|large workspace list|workspace lifecycle|trusted workspace"` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="workspace picker|large workspace list|workspace lifecycle|trusted workspace"</automated></verify>
  <done>The runtime exposes a scalable workspace picker and complete profile-management surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transactional safe per-chat workspace switching</name>
  <files>src/toolsProvider.ts, src/pluginTypes.ts, src/settings.ts, src/invocationRegistry.ts, test/toolsProvider.integration.test.ts, test/workspaceConfiguration.integration.test.ts</files>
  <read_first>src/toolsProvider.ts, src/preflight.ts, src/recoveryError.ts, src/invocationRegistry.ts, src/repositoryIdentity.ts, test/toolsProvider.integration.test.ts</read_first>
  <behavior>
    - Candidate profile is validated before active selection changes.
    - Disabled, deleted, untrusted, identity-mismatched, busy, or termination-locked profiles fail with structured recovery output.
    - Successful switch preserves the current Host/WSL environment, updates only the active chat selection, clears backend/workspace caches, and affects the next operation.
    - A failed switch leaves the previous active profile and backend untouched.
  </behavior>
  <action>Add `switch_workspace` as a transactional orchestration operation. Resolve candidate path for the current environment, validate lifecycle/trust/identity, consult the invocation registry, run preflight, then persist the active profile and invalidate cached workspace/backend state only after success. Return previous and new profile IDs plus the new status. Add multi-controller tests proving chat isolation and rollback on failure.</action>
  <acceptance_criteria>
    - Failed switches preserve the old active profile and backend context.
    - Successful switches affect the next command/filesystem operation without restarting the plugin.
    - Active invocation and unresolved termination block switching.
    - `npm test -- --test-name-pattern="workspace switch|safe switch|Host and WSL controllers|repository identity" && npm run build` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="workspace switch|safe switch|Host and WSL controllers|repository identity" && npm run build</automated></verify>
  <done>Users can switch workspaces seamlessly during chats without bypassing safety guarantees.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 6 regression and release verification</name>
  <files>test/workspaceConfiguration.integration.test.ts, test/toolsProvider.integration.test.ts, test/compatibility.test.ts</files>
  <read_first>package.json, scripts/verify-release.mjs, .planning/phases/06-workspace-profiles-and-safety/06-VALIDATION.md</read_first>
  <action>Close any missing assertions in the Phase 6 validation matrix, confirm intentional new tool names in compatibility coverage, and run the full project test, build, release, and diff gates.</action>
  <acceptance_criteria>
    - All 13 Phase 6 requirements have behavioral evidence.
    - `npm test`, `npm run build`, `npm run verify:release`, and `git diff --check` all exit 0.
  </acceptance_criteria>
  <verify><automated>npm test && npm run build && npm run verify:release && git diff --check</automated></verify>
  <done>Phase 6 is release-verified with no unresolved profile, picker, trust, lifecycle, or switching gaps.</done>
</task>
</tasks>

<verification>
- [ ] Picker, lifecycle, switching, and safety integration tests pass.
- [ ] Full suite, build, release verification, and diff checks pass.
- [ ] No unresolved plan-checker findings remain.
</verification>

## Artifacts this phase produces

- Paginated/searchable workspace picker tools
- Profile add/update/trust/preference/enable/lifecycle tools
- Transactional `switch_workspace`
- Large-list, lifecycle, trust, and safe-switch integration coverage

<output>After completion, create `06-03-SUMMARY.md`.</output>
