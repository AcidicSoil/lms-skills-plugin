interface:
  display_name: "Prompt Workflow Curator"
  short_description: "Curate PromptSnippets workflow libraries"
  default_prompt: "Use $prompt-workflow-curator to audit and curate this PromptSnippets library."
policy:
  allow_implicit_invocation: true
