---
phase: 01-execution-and-path-foundation
plan: 03
type: tdd
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [src/executor.ts, src/toolsProvider.ts, test/executor.test.ts, dist/]
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03]
user_setup: []
must_haves:
  truths:
    - Host execution preserves the existing shell-string contract without invalid-cwd home fallback.
    - WSL execution uses `wsl.exe` argv with explicit cwd/distribution and no silent Host fallback.
    - Timeout/output limits are shared and termination status is reported.
  artifacts:
    - src/executor.ts
    - test/executor.test.ts
  key_links:
    - src/executor.ts consumes execution settings and path-policy results from Plans 01 and 02.
    - src/toolsProvider.ts passes execution environment and distribution into the executor.
---

<objective>
Refactor command execution behind Host/WSL adapters while preserving `run_command` compatibility and enforcing strict cwd/process safety.

Purpose: Complete the executable Phase 1 foundation consumed by workspace-aware tools in Phase 2.
Output: Adapter-based executor, tool wiring, tests, and regenerated distribution artifacts.
</objective>

<execution_context>
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/workflows/execute-plan.md
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-execution-and-path-foundation/01-01-SUMMARY.md
@.planning/phases/01-execution-and-path-foundation/01-02-SUMMARY.md
@src/executor.ts
@src/toolsProvider.ts
@src/constants.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Test-drive Host and WSL execution requests</name>
  <files>src/executor.ts, test/executor.test.ts</files>
  <read_first>src/executor.ts, src/constants.ts, src/types.ts, src/pathPolicy.ts, src/wslCapability.ts</read_first>
  <action>Introduce an execution request contract with explicit environment and shell/direct modes. Preserve `execCommand(commandString, ...)` compatibility as shell mode. Host mode uses the existing shell resolver but rejects missing/invalid cwd when cwd is required. WSL shell mode spawns `wsl.exe` with argv containing optional `--distribution`, `--cd`, a known shell, and the unchanged command string as one shell argument. Direct mode uses `--exec program ...args`. Inject spawn/process dependencies for tests. Never parse arbitrary shell strings into argv and never fall back to Host after WSL failure.</action>
  <verify>npm test -- --test-name-pattern="executor"</verify>
  <acceptance_criteria>
    - WSL tests assert exact program and argv boundaries for spaces, quotes, Unicode, and metacharacters.
    - Invalid cwd produces a structured error and never `os.homedir()`.
    - Existing Host shell-string behavior remains tested.
  </acceptance_criteria>
  <done>SAFE-01 and command compatibility are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Unify limits, timeout cleanup, and tool wiring</name>
  <files>src/executor.ts, src/toolsProvider.ts, test/executor.test.ts</files>
  <read_first>src/executor.ts, src/toolsProvider.ts, src/settings.ts</read_first>
  <action>Share output truncation and timeout handling across adapters. Attempt platform-appropriate process-tree cleanup and add a result field indicating incomplete/unknown descendant termination rather than claiming success. Wire `run_command` to effective `executionEnvironment` and `wslDistribution`; update its description to remove home fallback and state that configured/explicit cwd errors fail. Do not migrate filesystem tools in this phase.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - Host and WSL paths use the same timeout and output constants.
    - Timeout tests verify `timedOut` and termination diagnostics.
    - `run_command` has no documented or implemented invalid-cwd home fallback.
    - Filesystem tools remain unchanged for Phase 2.
  </acceptance_criteria>
  <done>SAFE-02 and SAFE-03 are implemented without scope creep.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate distributable artifacts and run compatibility checks</name>
  <files>dist/</files>
  <read_first>package.json, tsconfig.json</read_first>
  <action>Run the complete test and build pipeline, regenerate `dist/` from `src/`, and inspect generated diffs for accidental API drift. Do not hand-edit generated files.</action>
  <verify>npm test && npm run build && git diff --check</verify>
  <acceptance_criteria>
    - dist/ reflects all changed source modules.
    - TypeScript build and automated tests pass.
    - No whitespace errors or unrelated generated changes remain.
  </acceptance_criteria>
  <done>Phase 1 execution foundation is release-buildable.</done>
</task>
</tasks>

<verification>
- [ ] npm test
- [ ] npm run build
- [ ] git diff --check
- [ ] No Host↔WSL silent fallback exists.
</verification>

<success_criteria>
- All safety requirements are implemented.
- Existing Host command behavior remains compatible except invalid-cwd fallback is intentionally removed.
- WSL invocation is argument-safe and testable without live WSL.
- Generated output is synchronized.
</success_criteria>

<output>
After completion, create `.planning/phases/01-execution-and-path-foundation/01-03-SUMMARY.md`
</output>
