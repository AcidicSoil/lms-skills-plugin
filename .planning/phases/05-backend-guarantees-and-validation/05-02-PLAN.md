---
phase: 05-backend-guarantees-and-validation
plan: 02
type: standard
wave: 2
depends_on: [05-01]
files_modified: [src/backend.ts, src/workspaceFs.ts, src/skillStore.ts, src/toolsProvider.ts, src/workspace.ts, test/workspaceFs.test.ts, test/skillStore.test.ts, test/toolsProvider.integration.test.ts]
autonomous: true
requirements: [BACK-01, BACK-02, BACK-03, TEST-03]
user_setup: []
must_haves:
  truths:
    - One selected backend owns command, filesystem, repository/tool discovery, and skill operations for an invocation.
    - WSL-native paths never reach Host filesystem APIs.
    - Repository and tool discovery use the same environment and distribution as command execution.
    - Skill workflows cannot silently cross from WSL to Host or vice versa.
  artifacts: [src/backend.ts, test/toolsProvider.integration.test.ts]
  key_links:
    - toolsProvider creates one backend context after preflight and shares it across tools.
    - workspaceFs and skillStore are selected by that backend rather than independently.
---

<objective>
Create one coherent Host/WSL backend boundary and route every workspace-facing workflow through it.
</objective>

<context>
@.planning/phases/05-backend-guarantees-and-validation/05-01-SUMMARY.md
@.planning/phases/05-backend-guarantees-and-validation/05-RESEARCH.md
@src/workspaceFs.ts
@src/skillStore.ts
@src/toolsProvider.ts
@src/workspace.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Introduce a selected workspace backend context</name>
  <files>src/backend.ts, src/workspaceFs.ts, src/workspace.ts, test/workspaceFs.test.ts</files>
  <read_first>src/workspaceFs.ts, src/workspace.ts, src/executor.ts, test/workspaceFs.test.ts</read_first>
  <behavior>
    - Host backend uses Host filesystem and Host command/program execution only.
    - WSL backend uses WSL program execution for all filesystem operations and never passes a WSL-native path to Node `fs`.
    - Backend context carries environment, distribution, native root, command executor, structured executor, and workspace filesystem.
  </behavior>
  <action>Create a `WorkspaceBackend` factory around resolved workspace context, executor functions, and `WorkspaceFileSystem`. Refactor callers to depend on this interface. Add sentinel tests whose Host filesystem stubs throw if invoked during WSL operations.</action>
  <acceptance_criteria>
    - WSL filesystem tests prove zero Host filesystem calls.
    - Host and WSL backends expose the same operation surface.
    - `npm test -- --test-name-pattern="workspace backend|workspace filesystem"` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="workspace backend|workspace filesystem"</automated></verify>
  <done>A single selected backend controls all workspace execution and filesystem behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route discovery and skill workflows through the selected backend</name>
  <files>src/backend.ts, src/skillStore.ts, src/toolsProvider.ts, test/skillStore.test.ts, test/toolsProvider.integration.test.ts</files>
  <read_first>src/skillStore.ts, src/toolsProvider.ts, src/backend.ts, test/skillStore.test.ts, test/toolsProvider.integration.test.ts</read_first>
  <behavior>
    - Repository/tool discovery runs in the selected backend workspace and distribution.
    - Host chat skill operations use Host roots; WSL chat skill operations use WSL roots and runners.
    - Command, filesystem, discovery, and skill operations report the same environment/workspace identity.
  </behavior>
  <action>Construct skill store and discovery helpers from the shared backend context. Remove or encapsulate direct environment rediscovery in individual tools. Add integration spies asserting the same environment, distribution, and native root flow through command, filesystem, discovery, and skill paths.</action>
  <acceptance_criteria>
    - Cross-surface integration tests fail on any environment/distribution mismatch.
    - No WSL discovery path is passed to Host `fs` or Host process cwd.
    - `npm test -- --test-name-pattern="backend routing|skill store|discovery" && npm run build` exits 0.
  </acceptance_criteria>
  <verify><automated>npm test -- --test-name-pattern="backend routing|skill store|discovery" && npm run build</automated></verify>
  <done>All workspace-facing workflows consistently use the selected backend.</done>
</task>
</tasks>

<verification>
- [ ] Backend and routing integration tests pass.
- [ ] `npm run build` passes.
- [ ] `git diff --check` passes.
</verification>

## Artifacts this phase produces

- `src/backend.ts`
- Shared Host/WSL backend factory and context
- Sentinel no-Host-API tests for WSL paths
- Cross-surface backend consistency tests

<output>After completion, create `05-02-SUMMARY.md`.</output>
