---
phase: 01-execution-and-path-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.test.json, src/types.ts, src/config.ts, src/settings.ts, src/wslCapability.ts, test/settings.test.ts, test/wslCapability.test.ts]
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, EXEC-04, EXEC-05, TEST-01]
user_setup: []
must_haves:
  truths:
    - Legacy settings resolve to Host execution without manual migration.
    - WSL readiness distinguishes unsupported host, unavailable WSL, no distribution, and ready states.
    - A selected distribution is validated and never silently replaced.
  artifacts:
    - src/wslCapability.ts
    - test/settings.test.ts
    - test/wslCapability.test.ts
  key_links:
    - src/settings.ts returns the execution fields defined in src/types.ts.
    - src/wslCapability.ts uses an injectable argument-based process runner.
---

<objective>
Add the backward-compatible execution configuration contract, test harness, and testable WSL capability detection.

Purpose: Establish explicit Host/WSL selection before execution routing changes.
Output: Typed settings, static LM Studio fields, capability service, and automated tests.
</objective>

<execution_context>
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/workflows/execute-plan.md
@/home/user/projects/lms-skills-plugin/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-execution-and-path-foundation/01-CONTEXT.md
@.planning/phases/01-execution-and-path-foundation/01-RESEARCH.md
@src/config.ts
@src/types.ts
@src/settings.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Establish the Node test harness and settings migration tests</name>
  <files>package.json, tsconfig.test.json, test/settings.test.ts</files>
  <read_first>package.json, tsconfig.json, src/settings.ts, src/types.ts</read_first>
  <action>Add a Node built-in test workflow that compiles TypeScript tests and runs them with `node --test`. Create tests proving missing execution fields normalize to `executionEnvironment: "host"`, existing skill/shell settings survive, and invalid persisted WSL values do not corrupt the effective config. Expose narrowly scoped dependency/reset hooks only where required for deterministic tests.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - package.json contains a working `test` script using Node's test runner.
    - test/settings.test.ts proves legacy settings select Host by default.
    - Existing `npm run build` remains green.
  </acceptance_criteria>
  <done>Settings migration behavior is executable and regression-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Add execution settings and WSL capability detection</name>
  <files>src/types.ts, src/config.ts, src/settings.ts, src/wslCapability.ts, test/wslCapability.test.ts</files>
  <read_first>src/config.ts, src/types.ts, src/settings.ts, .planning/phases/01-execution-and-path-foundation/01-CONTEXT.md</read_first>
  <action>Define `ExecutionEnvironment` as `"host" | "wsl"`; add `executionEnvironment` and optional `wslDistribution` to persisted/effective settings; default legacy data to Host. Add static config fields without inventing dynamic SDK APIs. Implement a Windows-only capability detector with a discriminated result for unsupported-platform, wsl-unavailable, no-distribution, distribution-unavailable, and ready. Invoke `wsl.exe` through an injected program/argv runner; never one interpolated shell string and never install or change WSL defaults.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - src/types.ts contains the explicit Host/WSL union.
    - src/settings.ts preserves old fields while defaulting missing execution mode to Host.
    - src/wslCapability.ts has no silent fallback to another distribution.
    - Tests cover all readiness states and argv boundaries.
  </acceptance_criteria>
  <done>Configuration and detection satisfy EXEC-01 through EXEC-05.</done>
</task>
</tasks>

<verification>
- [ ] npm test
- [ ] npm run build
- [ ] Capability tests do not require a live WSL installation.
</verification>

<success_criteria>
- All tasks completed.
- All verification checks pass.
- Legacy users remain in Host mode.
- WSL capability failures are actionable and typed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-execution-and-path-foundation/01-01-SUMMARY.md`
</output>
